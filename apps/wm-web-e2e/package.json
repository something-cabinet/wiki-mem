{
  "name": "wm-web-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npx playwright install chromium 2>/dev/null || true",
    "mock:start": "bun ../../packages/wm-mock-server/src/bun-entry.ts --mappings ./mappings --port 8081",
    "test": "codeceptjs run --steps",
    "test:headless": "HEADLESS=true codeceptjs run --steps",
    "test:e2e": "HEADLESS=true codeceptjs run --steps",
    "test:e2e:headed": "codeceptjs run --steps"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "codeceptjs": "^4.1.0",
    "playwright": "^1.50.0",
    "tsx": "^4.23.9"
  }
}
