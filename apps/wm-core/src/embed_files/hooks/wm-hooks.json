{
  "version": "v1",
  "hooks": [
    {
      "name": "wm-runtime-memory",
      "description": "Inject wiki context at session start",
      "trigger": "SessionStart",
      "action": {
        "type": "command",
        "command": "echo '=== Wiki Memory Engine (wm) ===' && wm-cli --version 2>/dev/null || echo 'wm-cli not found. Install: npm install -g @something-cabinet/wm-cli'"
      },
      "enabled": true
    }
  ]
}
