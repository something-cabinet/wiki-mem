# KNOWNS (Deprecated)

**Knowns has been fully migrated to WM.** This file is kept as a historical reference only.

**Canonical guidance is at `WIKI-MEM.md` in the repository root.** Read that first.

The `.knowns/` directory has been removed. All docs, memory, and tasks now live under `.wm/`. Use WM MCP tools (`wm_*`) for all operations.

For reference against the upstream [knowns-dev/knowns](https://github.com/knowns-dev/knowns) project, see `docs/migration-knowns-to-wm.md` (if it exists) or git history.
