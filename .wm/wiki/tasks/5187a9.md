---
title: "WT: Fix meta.path resolution consistency"
id: 5187a9
type: task
status: done
priority: high
tags: [spec:wiki-tool-reliability, bug]
relates_to:
  - {type: implements, target: wiki:specs:wiki-tool-reliability}
---

Fix graph/mod.rs line 68 to store meta.path relative to project root (.wm/wiki/...) instead of stripping wiki_dir. This makes all page CRUD commands resolve paths consistently from CWD (wiki root). Affects page CRUD, graph rebuild, version tracking, time tracking, task board.

## Implementation Notes

graph/mod.rs meta.path fix already in HEAD (fb28a96e+373e86a) — proven via RED→GREEN revert test. Real remaining B7 bug: resolve_id_to_path in page_path_helper.rs joined caller-supplied base (CLI passed wiki_dir) → doubled .wm/wiki/.wm/wiki/... path in 'wm-cli page delete'. Fixed to resolve from CWD per D4. New path_resolution_test.rs 2/2 (graph meta.path + full CLI CRUD loop incl. delete). Follow-up handled in CLI lane: create_engine()/page Create+Time handlers re-joined .wm/wiki — fixed there.
