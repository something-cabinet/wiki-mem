---
title: Rewrite enterprise-grade doc for realistic scale targets
id: d6a021
type: task
status: done
priority: medium
tags: [docs, architecture, cleanup]
acceptance_criteria:
  - text: "enterprise-grade.md is retargeted to 'native-feeling at 10k nodes' as a stretch goal instead of 100k nodes / 60fps"
  - text: "The document type is corrected to convention and its content reflects current architecture decisions accurately"
---

From @oracle review: Current enterprise-grade.md targets 100k nodes / 60fps against a 331-node, 25-edge reality. Also labeled as 'concept' not 'convention'. The wiki already has a concept page ('The Fantasy Benchmark') naming this problem. Retarget to 'native-feeling at 10k nodes' as a stretch goal. Update the convention doc to reflect current architecture decisions accurately.

## Implementation Notes

`.wm/wiki/conventions/enterprise-grade.md`: frontmatter `type` corrected from `concept` → `convention` (no per-type schema beyond the shared frontmatter; `conventions/` dir contains only this file — see `packages/wm-engine/src/models/page_type_model.rs` where convention parses as concept fallback). Scale table already retargeted to 10k nodes (60fps stretch). Architecture Summary rewritten to reflect the single-daemon design: `wm-server` owns the single EngineState, serves HTTP API + embedded Angular SPA, with `wm-cli mcp` as a thin MCP↔HTTP proxy and no Tauri; diagram updated and linked to the `wm-server-overrides-tauri-primary` ADR. D1 remains marked superseded. Noted pre-existing broken ref: `@wiki/core:enterprise-grade` used in core docs while file lives at `conventions/`.
