---
title: "GFX: Edge label quality pass"
id: 938712
type: task
status: done
priority: low
tags: [spec:graph-ui-fix, ux]
acceptance_criteria:
  - text: "LABEL_FONT constant replaces the hardcoded '10px sans-serif' string and is referenced by all edge-label rendering"
  - text: "Edge label contrast in dark mode meets the graph theme contrast requirement"
  - text: "LOD thresholds for edges (0.5) and node labels (0.3) are reviewed and documented with any adjustments"
---

Centralize hardcoded '10px sans-serif' font string into a single constant. Improve dark-mode contrast for edge labels. Review LOD thresholds (currently 0.5 for edges, 0.3 for node labels). Extract LABEL_FONT constant.

## Implementation notes (done 2026-08-08)
- Added `LABEL_FONT = '11px sans-serif'` const in `canvas-graph.directive.ts` (actual size in code was 11px) and referenced it in the label overlay `cssText`.
- Labels now use CSS custom properties directly (`var(--card)`, `var(--border)`, `var(--muted-foreground)` with `color-mix` alpha), so they are theme-reactive across light/dark without per-frame computed-style reads; added a 1px `--border` outline for definition.
- LOD thresholds documented and implemented: `EDGE_LABEL_MIN_ZOOM = 0.4` (edge labels hidden below this zoom) and `MAX_EDGE_LABELS = 120` (overlay cap for dense graphs). No node labels are drawn on the canvas (titles surface in the hover tooltip), so there is no node-label LOD threshold — noted in a doc comment.
- Verified: `tsc --noEmit` and `ng build` pass.
