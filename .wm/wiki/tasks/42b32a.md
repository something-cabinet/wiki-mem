---
title: Refactor wm-cli mcp to register handlers directly
id: 42b32a
type: task
status: done
priority: high
tags: [from-spec, mcp]
spec: specs/mcp-tool-registry-unification
relates_to:
  - {type: implements, target: wiki:specs:mcp-tool-registry-unification}
acceptance_criteria:
  - text: "wm-cli mcp calls register_all_tools directly instead of HTTP proxy handlers"
  - text: "MCP mode starts no background HTTP server and the ureq dependency is removed"
---

Replace HTTP proxy handlers with direct register_all_tools call. Remove HTTP server startup from MCP mode. Remove ureq dependency. wm-cli mcp becomes MCP-only — no background HTTP server.
