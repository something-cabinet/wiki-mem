---
title: Delete CodeceptJS dead directory (apps/wm-web-e2e)
id: b336c7
type: task
status: done
priority: medium
tags: [cleanup, testing, e2e]
acceptance_criteria:
  - text: "apps/wm-web-e2e/ (CodeceptJS) directory is deleted"
  - text: "All references to the CodeceptJS directory are removed or updated, and the WDIO e2e setup still works"
---

From @oracle review T-6: apps/wm-web-e2e/ (CodeceptJS) is dead weight — WDIO migration is done. Delete the directory and update any references.

## Resolution: DONE — directory deleted, e2e coverage replaced by the HTTP-API suite
- Deleted `apps/wm-web-e2e/` entirely (config, helpers, journeys, pages,
  mappings, `package.json`, `proxy.e2e.conf.json`, stale failed-screenshot
  `output/`, untracked `node_modules/`).
- The e2e direction decision is recorded as a mini-ADR in task 23138a; the
  replacement coverage lives in `apps/wm-core/tests/e2e_http.rs`.

## References updated
- `.gitignore` — removed the `apps/wm-web-e2e/output/` artifact entry (kept
  the generic `*.temp.mjs` codeceptjs entry).
- Root `package.json` — replaced `e2e` / `e2e:start` / `e2e:stop` scripts with
  `test:e2e` (`cargo test -p wm-core --test e2e_http`) and `test:stress`.
- `oxfile.toml` — removed the `mock` and `web-e2e` app entries (both depended
  on the deleted dir: `cwd = apps/wm-web-e2e` and its `proxy.e2e.conf.json`).
- CI (`.github/workflows/ci.yml`) — new `e2e-stress` job runs the HTTP-API +
  stress suites against a spawned wm-server.
- Historical wiki spec pages (e2e-migration, wm-mock-package, tauri-*) retain
  CodeceptJS mentions as historical records only; no live code references
  remain.

## Implementation Notes
- "the WDIO e2e setup still works" is moot: WDIO was already removed and the
  Tauri target is gone (see task 2c6a79).
- `packages/wm-mock-server` remains on disk (out of scope) but is now orphaned.
