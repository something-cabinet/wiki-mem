---
title: "Web UI: Sim UI, icons, dark mode, animations"
type: task
id: 4c97b5
page_type: task
status: done
priority: medium
tags:
  - web-ui
  - angular
acceptance_criteria:
  - text: "Sim UI components are installed and used across the views"
  - text: "Emoji icons are replaced with Lucide SVGs and dark mode works across all views with a toggle"
  - text: "Loading shimmers, page transitions and hover effects are added, and Tailwind config is extended with a custom theme"
---
# Web UI: Sim UI, icons, dark mode, animations

Install Sim UI, Lucide icons, implement dark mode, add animations.

## Acceptance Criteria

- [x] Sim UI components installed and used
- [x] Emoji icons replaced with Lucide SVGs
- [x] Dark mode works across all views with toggle
- [x] Loading shimmers, page transitions, hover effects
- [x] Tailwind config extended with custom theme

## Status — DONE

- **Sim UI:** helm/spartan components in use across all views (button, card, badge, alert, select, tabs, accordion, switch, sidebar, tooltip, skeleton). ✓
- **Lucide icons:** all icons via `@ng-icons/lucide` (`NgIcon`) — no emoji. ✓
- **Dark mode:** toggle in sidebar footer + header, `prefers-color-scheme` detection, flash-prevention script in `index.html`, custom theme vars in `styles.css`. ✓
- **Loading shimmers:** `wm-skeleton` shimmer placeholders in all 6 views (prior lane). ✓
- **Hover effects:** present (cards/buttons `hover:*` transitions). ✓
- **Page transitions — NEW (the remaining gap):** added a CSS-only enter animation — `.wm-page-enter` class (fade + 6px slide-up, 0.22s ease-out) in `styles.css`, applied to the root element of tasks/pages/memory/search/graph/settings/code views. No animation library; respects `prefers-reduced-motion` via the existing global reduced-motion block.
- **Custom theme:** Tailwind v4 `@theme inline` + CSS custom properties in `styles.css`. ✓
