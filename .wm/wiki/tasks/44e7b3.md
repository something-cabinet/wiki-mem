---
title: Add ServerHandler impl for Arc<ToolRegistry>
id: 44e7b3
type: task
status: cancelled
priority: high
tags: [from-spec, mcp]
spec: specs/mcp-tool-registry-unification
relates_to:
  - {type: implements, target: wiki:specs:mcp-tool-registry-unification}
acceptance_criteria:
  - text: "impl ServerHandler for Arc<ToolRegistry> exists in transport.rs and delegates every trait method to the inner ToolRegistry"
  - text: "A single ToolRegistry instance is shareable between the MCP and HTTP serving paths"
---

Add `impl ServerHandler for Arc<ToolRegistry>` in transport.rs that delegates all trait methods to the inner ToolRegistry. This is the prerequisite for sharing a single registry between MCP and HTTP paths.
