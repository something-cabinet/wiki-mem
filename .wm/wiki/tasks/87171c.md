---
title: 'GFX: Remove d3-force, fjadra-only layout'
id: 87171c
type: task
status: done
priority: medium
tags:
- spec:graph-ui-fix
- cleanup
acceptance_criteria:
- text: d3-force, d3-zoom, and d3-selection are removed from package.json dependencies
- text: Canvas directive no longer imports d3-force or holds Simulation type/state, and startSimulation()/zoomBy()/fitToView() are removed
- text: render() is exposed via a public triggerRender() method that is called after applyPositions from fjadra
---

Remove d3-force dependency from canvas directive and package.json:
- Remove d3-force imports, Simulation types, simulation state
- Remove startSimulation(), updateLinkDistance(), zoomBy(), fitToView() (or reimplement)
- Add public triggerRender() method
- After applyPositions from fjadra, call triggerRender()
- Remove d3-force, d3-zoom, d3-selection from package.json deps