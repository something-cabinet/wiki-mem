---
title: 'GFX: Wire WebGL toggle + data binding'
id: cb2799
type: task
status: cancelled
priority: medium
tags:
- spec:graph-ui-fix
- webgl
acceptance_criteria:
- text: Settings Appearance section includes a 'Use WebGL rendering' toggle
- text: WebGL auto-detection switches the renderer when >500 nodes, and detection happens after data arrival rather than in ngAfterViewInit
- text: 'Remaining WebGL data bindings wired: node sizes, label SDF text, and camera sync; the toggle produces correct output using --page-type-* tokens'
---

1. Add "Use WebGL rendering" toggle in Settings Appearance section
2. Auto-detect: switch to WebGL when >500 nodes
3. Move WebGL detection from ngAfterViewInit (too early) to after data arrival
4. Wire remaining WebGL data bindings: node sizes, label SDF text, camera sync
5. WebGL renderer already correctly reads --page-type-* tokens — ensure toggle produces correct output