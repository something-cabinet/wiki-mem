---
title: "WT: Wire From&lt;ToolError&gt; for ErrorData conversion in transport.rs"
id: 641eeb
type: task
status: done
priority: high
tags: [spec:wiki-tool-reliability, mcp, errors]
relates_to:
  - {type: implements, target: wiki:specs:wiki-tool-reliability}
acceptance_criteria:
  - text: "MCP error responses include structured {code, message, hint} in JSON-RPC error.data via ErrorData::from(err) in apps/wm-cli/src/mcp_transport.rs"
  - text: "error_data_transport_test.rs passes: NOT_FOUND maps to -32602 and invalid input maps to -32603, with the full test suite green"
---

Wire the existing From&lt;ToolError&gt; for ErrorData impl (already in wm-error) into transport.rs so MCP error responses include code, message, and hint as structured JSON-RPC error.data. Currently only err.message is sent as text.

## Implementation Notes

Real boundary is apps/wm-cli/src/mcp_transport.rs (McpServer::call_tool), not wm-core transport.rs. Handler-error arm now returns Err(ErrorData::from(err)) → structured {code, message, hint} in JSON-RPC error.data. helpers/mcp.rs: extracted send_request_raw() (additive). New error_data_transport_test.rs 2/2 (NOT_FOUND → -32602, invalid input → -32603). RED→GREEN confirmed. Full suite green.
