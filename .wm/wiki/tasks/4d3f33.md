---
title: Add system dark mode detection via prefers-color-scheme
id: 4d3f33
type: task
status: done
priority: high
acceptance_criteria:
  - text: "settings-view.component.ts falls back to window.matchMedia('(prefers-color-scheme: dark)') when localStorage has no saved theme preference"
  - text: "Theme updates in real time when the system color scheme changes via the matchMedia change listener"
---

In settings-view.component.ts, use `window.matchMedia('(prefers-color-scheme: dark)')` as fallback when localStorage has no saved preference. Listen for changes to update in real-time.
