---
title: Add wildcard route to redirect invalid URLs to search
id: cf486e
type: task
status: todo
priority: high
acceptance_criteria:
  - text: "app.routes.ts includes { path: '**', redirectTo: '/search' }"
  - text: "Invalid URLs redirect to the search view instead of rendering a blank layout"
---

Add `{ path: '**', redirectTo: '/search' }` to app.routes.ts to prevent blank layout on invalid URLs.
