---
title: "WT: Add JSON schema annotations + remove page_id param"
id: 3d5c67
type: task
status: done
priority: high
tags: [spec:wiki-tool-reliability, mcp, schemas]
relates_to:
  - {type: implements, target: wiki:specs:wiki-tool-reliability}
---

1. Add #[schemars(description="...")] annotations to all wm_page.* action fields for better tool discovery
2. Remove page_id parameter entirely, canonicalize to id only
3. Verify tools/list returns complete schemas

## Implementation Notes

Core fixes (schemars descriptions on all WmPageAction fields + id-only params) already present in page/action.rs — verified end-to-end via live tools/list. Removed last page_id identifier in page tool module (Create handler local var). Added test_regression_wm_page_uses_id_parameter, test_regression_wm_page_get_by_id, test_regression_wm_page_schema_complete (7 oneOf arms, every field documented, required arrays complete, no page_id). Unblocked other-lane test compile breaks (cli_run.rs Write import, stress_test.rs import, e2e_code_intel.rs rebuild_code_index API migration). page_id census: ~85 remaining matches are internal data-model fields, not MCP params.
