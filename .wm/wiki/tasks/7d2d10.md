---
title: Graph UI Polish — Node Spacing, Sizing, and Layout
id: 7d2d10
type: task
status: done
priority: high
tags:
  - web-ui
  - graph
  - ux
acceptance_criteria:
  - text: "Non-connected nodes stay within a reasonable viewport distance instead of drifting to the edges"
  - text: "Default node size is increased so labels are readable at default zoom"
  - text: "The spacing slider controls spacing for ALL nodes (connected and unconnected); build passes"
---

Fix three graph visualization issues affecting readability: unconnected nodes drift too far, nodes are too small, and the spacing slider only affects connected nodes instead of all nodes.

## Acceptance Criteria

- [x] Non-connected nodes stay within reasonable viewport distance (not scattered to edges)
- [x] Default node size increased (currently too small to read labels)
- [x] Spacing slider controls spacing for ALL nodes (connected + unconnected), not just connected pairs
- [x] Build passes

## Implementation notes (done 2026-08-08)
- AC1 (golden-angle spread + center force + fitToView) and AC2 (radius `max(18, min(55, sqrt(deg)*8 + 10))`) were already shipped in the prior commit for this task; this pass completed the remaining work.
- AC3 was blocked on 2ec7a3: the spacing slider now drives fjadra global ManyBody repulsion (`charge_strength`) in addition to link distance, so it affects every node — connected and unconnected. See 2ec7a3 notes.
- Verified: `tsc --noEmit` and `ng build` pass.
