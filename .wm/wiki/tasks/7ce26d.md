---
title: Fix 5 wiki tool bugs — match-arm value discarding in mcp/tools/page.rs
id: 7ce26d
type: task
status: todo
priority: urgent
tags: [bug, mcp, tool-reliability, urgent]
acceptance_criteria:
  - text: "wm_page.update no longer returns a phantom 'page not found' when the page exists on disk"
  - text: "Match arms in mcp/tools/page.rs no longer silently discard values (tags: _ and similar) — frontmatter fields such as tags are preserved through all wm_page/wm_index operations"
  - text: "page_id vs id confusion resolved (both keys handled consistently) and all required parameters for wm_page/wm_index tools are documented in input schemas"
---

From @oracle review T-1: 5 documented bugs in wm_page/wm_index (phantom 'page not found' on update, page_id vs id confusion, undocumented required params). Root cause: match arms in mcp/tools/page.rs silently discarding values (tags: _ instead of tags). Every agent session hits these — the filesystem-write workaround violates the MCP-first principle.
