---
title: "Update wm-init skill to load wiki rules at session start"
id: 23b628
type: task
status: done
severity: Medium
spec: wiki-rules-auto-load
fulfills: FR-1, FR-2, FR-3, FR-4, FR-5
relates_to:
  - {type: implements, target: wiki:specs:wiki-rules-auto-load}
acceptance_criteria:
  - text: "wm-init skill includes a dedicated 'Load Wiki Rules' step that discovers rule pages from .wm/wiki/rules/ using MCP tools first with a file-read fallback"
  - text: "Session context output includes a 'Rules' section and the validation step checks work against active rules"
---

## Task: Update wm-init skill

Add a "Step 4.5: Load Wiki Rules" to the wm-init skill that:
- Discovers all rule pages from `.wm/wiki/rules/` (MCP-first, file fallback)
- Reads each rule and summarizes in session context
- Adds a validation check against active rules before task completion

### Acceptance Criteria
- [x] wm-init includes a dedicated step for loading wiki rules
- [x] Uses MCP tools first, falls back to direct file reads
- [x] Session context output includes a "Rules" section
- [x] Validation step checks work against active rules

## Implementation Notes

Added "Step 4.5: Load Wiki Rules" to `apps/wm-core/src/embed_files/skills/wm-init/SKILL.md` (canonical) between Step 4 (task board) and Load Critical Learnings: MCP-first (`wm_page.list` type=rule + `wm_page.get`), file-read fallback (`.wm/wiki/rules/*.md`), no-crash on missing/empty rules dir, and a rules-check before task completion. Renumbered subsequent steps (5–8); added "Rules" line to Session Context, two checklist items, two red flags, and active-rules in the Final Response Contract. Mirrored to `.claude/skills/wm-init/`, `.opencode/skills/wm-init/`, `.kiro/skills/wm-init/` (all verified byte-identical). Note: `.agent/skills/` (referenced by the spec) is empty — no copy exists there to mirror.
