---
title: Replace hardcoded graph node colors with theme-aware values
id: b563fd
type: task
status: todo
spec: specs/webgl-graph-rendering
priority: medium
relates_to:
  - {type: implements, target: wiki:specs:webgl-graph-rendering}
acceptance_criteria:
  - text: "Hardcoded hex node colors in canvas-graph.directive.ts are replaced with values read from CSS custom properties"
  - text: "The white node stroke uses var(--ring) so it renders correctly across light and dark themes"
---

In canvas-graph.directive.ts, replace hardcoded hex node colors with values from CSS custom properties, and change white stroke to use `var(--ring)` for reliable cross-theme appearance.
