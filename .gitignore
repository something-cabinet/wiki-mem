# Rust
target/

# Editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Environment / secrets
.env
.env.*
!.env.example

# SvelteKit build output
.svelte-kit/

# Knowns (migrated to .wm/ — no longer needed)
.knowns/
refs/knowns

# Wiki Memory Engine
# Only ignore runtime artifacts, not the wiki content itself
# synced from embedded binary
**/.wm/skills/
# runtime audit log
**/.wm/log.jsonl
# runtime log files
**/.wm/log/
**/.wm/audit.jsonl
# per-launch web API token (secret)
**/.wm/state/web-token
# per-launch MCP proxy token (secret)
**/.wm/state/mcp-token
# per-launch server discovery record (port/pid/started_at)
**/.wm/server.json
# stray absolute path artifact (Windows)
c/

# Runtime ghost .wm/ dirs (from EngineState using CWD instead of project root)
apps/wm-core/.wm/
apps/wm-cli/.wm/
apps/wm-web/.wm/
apps/wm-web/src-tauri/.wm/

# Temp files
tmp/

# codeceptjs TS transpile temp files
*.temp.mjs

# Deepwork progress — local-only, not committed
.slim/deepwork/

# Platform-specific AI tooling (regenerated by `wm setup`)
.codex/
.kiro/
.github/copilot-instructions.md
.claude/