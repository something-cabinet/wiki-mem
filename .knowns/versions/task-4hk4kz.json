{
  "taskId": "4hk4kz",
  "currentVersion": 3,
  "versions": [
    {
      "id": "v1",
      "taskId": "4hk4kz",
      "version": 1,
      "timestamp": "2026-07-07T03:59:23.7827542Z",
      "changes": [
        {
          "field": "status",
          "oldValue": null,
          "newValue": "todo"
        },
        {
          "field": "priority",
          "oldValue": null,
          "newValue": "high"
        }
      ],
      "snapshot": {
        "description": "Match Knowns' `knowns search` and `knowns retrieve` capabilities: a single search call that queries across wiki pages, tasks, memory entries, and docs simultaneously, with hybrid RRF fusion (BM25 + semantic) and ranked results.\n\nCurrent WM search only queries wiki pages. Tasks and memory are separate non-searchable lists.\n\nWhat's needed:\n1. **Unified SearchIndex** — BM25 index that spans pages + tasks + memory + docs, each tagged with their entity type\n2. **wm_search.query `type` filter** — already exists for pages, extend to filter memory/tasks/docs or \"all\"\n3. **wm_search.retrieve** — extend to assemble context from any entity type, not just pages\n4. **Hybrid default** — RRF fusion on by default (BM25 + semantic embeddings) when embed feature is enabled\n5. **Cross-entity results** — single ranked list mixing pages, tasks, memory snippets, and doc excerpts with type labels\n6. **Tests** — integration tests for cross-entity search, hybrid ranking, result labeling",
        "labels": [
          "memory",
          "search",
          "knowns",
          "parity"
        ],
        "priority": "high",
        "status": "todo",
        "title": "Cross-entity hybrid search (pages + tasks + memory + docs)"
      }
    },
    {
      "id": "v2",
      "taskId": "4hk4kz",
      "version": 2,
      "timestamp": "2026-07-07T04:42:25.2379736Z",
      "changes": [
        {
          "field": "implementationNotes",
          "oldValue": null,
          "newValue": "Spec approved. 8 decisions locked, 12 ACs, all Oracle findings resolved. ScoringConfig with 14 parameters in config.json search.scoring."
        }
      ],
      "snapshot": {
        "description": "Match Knowns' `knowns search` and `knowns retrieve` capabilities: a single search call that queries across wiki pages, tasks, memory entries, and docs simultaneously, with hybrid RRF fusion (BM25 + semantic) and ranked results.\n\nCurrent WM search only queries wiki pages. Tasks and memory are separate non-searchable lists.\n\nWhat's needed:\n1. **Unified SearchIndex** — BM25 index that spans pages + tasks + memory + docs, each tagged with their entity type\n2. **wm_search.query `type` filter** — already exists for pages, extend to filter memory/tasks/docs or \"all\"\n3. **wm_search.retrieve** — extend to assemble context from any entity type, not just pages\n4. **Hybrid default** — RRF fusion on by default (BM25 + semantic embeddings) when embed feature is enabled\n5. **Cross-entity results** — single ranked list mixing pages, tasks, memory snippets, and doc excerpts with type labels\n6. **Tests** — integration tests for cross-entity search, hybrid ranking, result labeling",
        "implementationNotes": "Spec approved. 8 decisions locked, 12 ACs, all Oracle findings resolved. ScoringConfig with 14 parameters in config.json search.scoring.",
        "labels": [
          "memory",
          "search",
          "knowns",
          "parity"
        ],
        "priority": "high",
        "status": "todo",
        "title": "Cross-entity hybrid search (pages + tasks + memory + docs)"
      }
    },
    {
      "id": "v3",
      "taskId": "4hk4kz",
      "version": 3,
      "timestamp": "2026-07-07T06:19:51.5250939Z",
      "changes": [
        {
          "field": "status",
          "oldValue": "todo",
          "newValue": "done"
        },
        {
          "field": "implementationNotes",
          "oldValue": "Spec approved. 8 decisions locked, 12 ACs, all Oracle findings resolved. ScoringConfig with 14 parameters in config.json search.scoring.",
          "newValue": "Spec approved. 8 decisions locked, 12 ACs, all Oracle findings resolved. ScoringConfig with 14 parameters in config.json search.scoring.\nAll 12 ACs implemented in this session. Cross-entity search, type filter, RRF merge, FSRS recency, salience boost, memory retrieve, per-type status, IndexScheduler."
        }
      ],
      "snapshot": {
        "description": "Match Knowns' `knowns search` and `knowns retrieve` capabilities: a single search call that queries across wiki pages, tasks, memory entries, and docs simultaneously, with hybrid RRF fusion (BM25 + semantic) and ranked results.\n\nCurrent WM search only queries wiki pages. Tasks and memory are separate non-searchable lists.\n\nWhat's needed:\n1. **Unified SearchIndex** — BM25 index that spans pages + tasks + memory + docs, each tagged with their entity type\n2. **wm_search.query `type` filter** — already exists for pages, extend to filter memory/tasks/docs or \"all\"\n3. **wm_search.retrieve** — extend to assemble context from any entity type, not just pages\n4. **Hybrid default** — RRF fusion on by default (BM25 + semantic embeddings) when embed feature is enabled\n5. **Cross-entity results** — single ranked list mixing pages, tasks, memory snippets, and doc excerpts with type labels\n6. **Tests** — integration tests for cross-entity search, hybrid ranking, result labeling",
        "implementationNotes": "Spec approved. 8 decisions locked, 12 ACs, all Oracle findings resolved. ScoringConfig with 14 parameters in config.json search.scoring.\nAll 12 ACs implemented in this session. Cross-entity search, type filter, RRF merge, FSRS recency, salience boost, memory retrieve, per-type status, IndexScheduler.",
        "labels": [
          "memory",
          "search",
          "knowns",
          "parity"
        ],
        "priority": "high",
        "status": "done",
        "title": "Cross-entity hybrid search (pages + tasks + memory + docs)"
      }
    }
  ]
}
